#include "print_helpers.h"

// Include necessary headers for implementations below
#include <iostream>
#include <string>

// --- Implementations for Non-Template Functions ---

// If you had declared non-template functions in the header,
// their definitions would go here.
// Example:
/*
void printSeparatorLine() {
    std::cout << "----------------------------------------" << std::endl;
}
*/

// For now, if you only have the template function printVector defined
// entirely in the header, this file might not contain much else.
// It's still good practice to have it as part of the structure.
